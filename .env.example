# ResearchQuest env stub — the real template lives in researchquest/.env.example.
# Copy it to researchquest/.env and fill in your Supabase project values:
#   cp researchquest/.env.example researchquest/.env
# Required: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY
