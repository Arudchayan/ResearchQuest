# ponytail-audit — ResearchQuest (pass 1)

Ranked, biggest cut first. Every finding verified by repo-wide grep this pass.
Prior passes (AUDIT_2/AUDIT_4) applied some cuts; several flagged files still
remain and are re-listed here with current line counts.

1. `delete:` openapi.fixture.yaml — 491-line hand-committed copy of the OpenAPI doc the contract test generates live (`getOpenApiDocument`); CI only runs `test -f` on it. Delete fixture + the 2-line CI step. Nothing. [-493] [supabase/functions/api/openapi.fixture.yaml, .github/workflows/ci.yml:64-65]
2. `yagni:` Redesign visual-QA harness — playwright.redesign.config.ts + e2e/redesign-visual.spec.ts, gated behind undocumented `RQ_VISUAL_QA=1`, referenced by no docs/script/CI; evidence capture for an already-shipped redesign. Delete both; rerun from git history if ever needed again. Nothing. [-154] [researchquest/playwright.redesign.config.ts, researchquest/e2e/redesign-visual.spec.ts]
3. `delete:` AddIdeaDialog + its security test — sole importer of the dialog is its own test; IdeasBoard creates ideas inline (IdeasBoard.tsx). Nothing. [-146] [researchquest/src/components/ideas/AddIdeaDialog.tsx, researchquest/src/test/components/AddIdeaDialogSecurity.test.tsx]
4. `delete:` Skeleton dead variants — NoteCardSkeleton, IdeaCardSkeleton, TaskCardSkeleton, EditorSkeleton, SidebarSkeleton, SearchResultSkeleton, EmptyStateSkeleton all have zero importers (only base Skeleton, PaperCardSkeleton, ListSkeleton, AppLoadingSkeleton used). Nothing. [-145] [researchquest/src/components/ui/Skeleton.tsx]
5. `yagni:` Gamification store write-actions `activateBoost`/`consumeFreeze`/`useRestDay` — zero production call sites; awardXP mutates freeze tokens server-side itself, RightSidebar reads state only, `clearBoostLocally` used solely by a test. Keep hydrate/countdown/read; delete the three actions and payload plumbing. Nothing. [-95] [researchquest/src/store/gamificationStore.ts]
6. `delete:` ui/NotFound.tsx — both exports (`NotFound`, `ItemNotFound`) have zero importers; App.tsx renders its own inline recovery UI. Nothing. [-94] [researchquest/src/components/ui/NotFound.tsx]
7. `delete:` useGamificationDashboard hook — zero importers; Dashboard reads stores directly (Dashboard.tsx:20). Nothing. [-80] [researchquest/src/hooks/useGamificationDashboard.ts]
8. `delete:` Prod-build-in-Vitest harness — viteProductionBuild.test.ts shells out to full Vite builds just to assert prod bundles lack `data-matrix` markers emitted by serve-only vite-plugin-source-identifier; no app code reads the attribute. Delete test + vitest.build.config.ts + `test:build` script + the plugin dep/config. CI's plain `build` step covers it. Nothing. [-78, -1 dep] [researchquest/src/test/viteProductionBuild.test.ts, researchquest/vitest.build.config.ts, researchquest/package.json:17,86, researchquest/vite.config.ts:5,33-38]
9. `yagni:` usePaperSearchInternal — 70-line hook with exactly one caller (AddPaperView). Inline loading/error/results state into the caller. Nothing. [-55] [researchquest/src/hooks/usePaperSearchInternal.ts]
10. `yagni:` Parallel strictness regime — tsconfig.strict.json + `typecheck:strict` script + src/strict-gate/pass.ts/fail.fixture.ts canaries; script wired into nothing, fail fixture excluded from every tsconfig so the gate can't bite by construction. Fold flags into tsconfig.app.json once, or delete dir + config + script line. Nothing. [-42] [researchquest/tsconfig.strict.json, researchquest/src/strict-gate/, researchquest/src/../package.json:23]
11. `yagni:` Dead gamification content — XP_REWARDS.ADD_PAPER_INSIGHTS/DAILY_TASK_COMPLETION have no read sites; GOAL_CRUSHER branch waits on action `"complete_goal"` which nothing emits (goals feature removed); RESEARCH_HERO never awarded; papers_with_insights_count tracked for a dead counter. Update award-test keys in same change. Nothing. [-45] [researchquest/src/utils/gamification.ts:14,19,41-46,53-58,384-391]
12. `shrink:` Router selection switch — selectEntityForRoute is five near-identical loading-guard/find-by-id branches; a `Record<AppView, {loading,items,set}>` table collapses it, and `ParsedRoute.isValid` is derivable from `view !== null`. Table-driven map. [-40] [researchquest/src/lib/router.ts:130-173,44-48]
13. `yagni:` useEntityCrud knobs with ≤1 caller — override primitives `delete?:`/`restore?:` never overridden by any entity; `xpUpdate` used only by notes; `afterCreate` only by papers; `skipXpToast` plumbing serves one site. Fold defaults into the factory, move one-off bits into their single hooks. Shared default path. [-35] [researchquest/src/hooks/useEntityCrud.ts:92-99,113-121, src/hooks/useNotes.ts:104, src/hooks/usePapers.ts:319]
14. `native:` DOMPurify re-sanitizing HTML already produced by react-markdown + rehype-sanitize in copy/print handlers. Assign `previewRef.current.innerHTML` directly; drop dep. [-1 dep, -10] [researchquest/src/components/editor/hooks/useEditorActions.ts:4,38,82]
15. `native:` CVA + Radix Slot machinery — label.tsx uses `cva` with zero variants; `<Button asChild>` never rendered; button `secondary` variant never set. Flat class-string map + plain forwardRef components; drop `class-variance-authority` + `@radix-ui/react-slot`. [-2 deps, -20] [researchquest/src/components/ui/button.tsx, researchquest/src/components/ui/label.tsx]
16. `native:` Second icon library — `@radix-ui/react-icons` supplies 11 icons in 4 files while lucide-react serves 30 other files. Convert imports to lucide equivalents; drop dep. [-1 dep] [researchquest/src/components/dashboard/Dashboard.tsx:18-22, src/components/auth/AuthScreen.tsx:2, src/components/layout/v2/AppShell.tsx:6, src/components/layout/v2/Sidebar.tsx:9]
17. `native:` rimraf devDep for what Node 22 scripts do with `rm -rf`. `rm -rf node_modules/.vite-temp`. [-1 dep] [researchquest/package.json:8,9,14]
18. `delete:` Dead UI surface — CardTitle/CardDescription/CardFooter in card.tsx never imported elsewhere. Nothing. [-22] [researchquest/src/components/ui/card.tsx:32-75,76]
19. `delete:` Hook-return refresh API — refreshNotes/refreshIdeas/refreshPapers/refreshTasks exposed but never consumed by any component (incl. owner/non-owner branch in useTasks serving only this). Nothing. [-10] [researchquest/src/hooks/useNotes.ts:142, src/hooks/useIdeas.ts:223, src/hooks/usePapers.ts:610, src/hooks/useTasks.ts:200,324]
20. `stdlib/shrink:` estimateReadingTime duplicated — text.ts export has no production importer; useMarkdownEditor re-implements it verbatim inline. Consume the util at the editor, delete the inline copy. [-9] [researchquest/src/utils/text.ts:10-18, researchquest/src/components/editor/hooks/useMarkdownEditor.ts:57-63]
21. `delete:` logger.log() method — zero callers; warn has exactly 3 sites. Keep error/warn only. [-8] [researchquest/src/utils/logger.ts:16-21]
22. `delete:` xp-gain keyframe/animation in tailwind.config.js — no `animate-xp-gain` anywhere (fade-in/slide-in are used). Nothing. [-8] [researchquest/tailwind.config.js:204,222-225]
23. `shrink:` Every npm script prefixes `pnpm install --prefer-offline` (7 scripts) — the installer's job (CI installs explicitly), not the scripts'. Bare commands. [-7] [researchquest/package.json:7-13,15]
24. `delete:` Stale ANALYZE stanza in .env.example documenting rollup-plugin-visualizer, which isn't installed. Nothing. [-3] [researchquest/.env.example:33-35]
25. `shrink:` pnpm-workspace.yaml onlyBuiltDependencies allowlists `deno`/`supabase` npm packages that aren't dependencies (Deno arrives via setup-deno action). Keep esbuild; drop both entries. [-2] [researchquest/pnpm-workspace.yaml]

Kept deliberately: demoSupabase + demoData (~1,500 lines) behind VITE_DEMO_MODE is a documented, tested product feature; fonts/tailwindcss-animate/@tailwindcss/typography verified in index.css/tailwind.config.js; cmdk, sonner, @tanstack/react-virtual, useFilteredList, useUndoDelete all have 2+ production call sites; pnpm.overrides range floors are transitive-security floors (exact @codemirror pins redundant with direct deps but harmless).

net: -1612 lines, -6 deps possible.
