.browser_screenshots/

# Local env with secrets (never commit)
.env
.env.local

# Cursor / MCP browser scratch logs
.playwright-mcp/

# Manual Playwright / scratch verification (screenshots, videos); do not commit.
/verification/

# Duplicate path casing collides with `.jules/palette.md` on Windows; canonical file is under `.jules/`.
.Jules/palette.md

# Agent journals live only at repo root `.jules/` (merged from former `researchquest/.jules/`).
researchquest/.jules/

# Generated audit reports from portfolio expert cron jobs
researchquest/UI_UX_AUDIT.md
*.audit-report.md

# Ignore root-level npm/pnpm install artifacts (should only run in researchquest/)
node_modules/
.vite/
package-lock.json
