# ponytail-audit — ResearchQuest (pass 4)

Ranked, biggest cut first. Verified by import-graph scan across `researchquest/src`, `supabase/functions`, CI workflows, and docs (tests included only when they're the sole consumer). Nothing applied.

- `native:` Seven dependencies the toolchain already covers: `date-fns` (zero imports), `@types/react-router-dom` (react-router isn't installed), `happy-dom` (vitest runs jsdom), `rollup-plugin-visualizer` (never imported in vite.config.ts; the `ANALYZE` var documented in .env.example is a no-op), `supabase` CLI (no script/doc invokes it), `rimraf` (`rm -rf` in npm scripts), `vite-plugin-source-identifier` (its only consumer is the test asserting its own `data-matrix` markers). Remove from package.json; delete the stale `ANALYZE` stanza and the marker assertions in viteProductionBuild.test.ts. [-7 deps, ~25 lines] [researchquest/package.json, researchquest/.env.example, researchquest/src/test/viteProductionBuild.test.ts]
- `delete:` openapi.fixture.yaml (491 lines) is never read — the contract test generates the spec from _shared/openapi.ts, and CI's only check is `test -f`. Generate at test time instead of keeping a static duplicate. [researchquest/supabase/functions/api/openapi.fixture.yaml]
- `delete:` Agent-session journals and one-off evaluation snapshots (.jules/*.md learning logs, docs/evaluations/2026-07-21-*.md) — scratch state, tracked in git. Git history, not the working tree. [/.jules/, /docs/evaluations/]
- `delete:` Redesign visual-QA rig — e2e/redesign-visual.spec.ts (122) + playwright.redesign.config.ts (32); gated behind RQ_VISUAL_QA which no doc, script, or workflow ever sets, snapshots dir is empty/untracked, and the spec already skips under the main playwright.config. Delete until someone runs it. [-154 lines] [researchquest/e2e/redesign-visual.spec.ts, researchquest/playwright.redesign.config.ts]
- `delete:` ui/NotFound.tsx (94 lines) — zero importers; App.tsx renders its own inline not-found UI at src/App.tsx:305. Nothing replaces it; delete. [researchquest/src/components/ui/NotFound.tsx]
- `delete:` useGamificationDashboard hook (80 lines) — zero importers anywhere (src, tests, docs, README). Nothing. [researchquest/src/hooks/useGamificationDashboard.ts]
- `yagni:` useEntityCrud one-caller knobs — `updateGuard`, `buildOptimisticEntity`, `onCreateNullData` each set only by useIdeas, `onSnapshotMissing` only by useNotes. Fold these four branches into their single caller hooks; keep the shared default path. [~-60 lines] [researchquest/src/hooks/useEntityCrud.ts, researchquest/src/hooks/useIdeas.ts, researchquest/src/hooks/useNotes.ts]
- `yagni:` Four overlapping SPA-fallback configs for hosts only one of which is real: public/.htaccess (Apache), public/_redirects (Netlify), public/404.html (GH-pages meta-refresh), public/vercel.json (copied into dist where Vercel ignores it — root researchquest/vercel.json has no rewrites). Keep the deploy target's mechanism, delete the rest. [researchquest/public/]
- `delete:` Stray screenshot auth-gate-mobile.png at repo root — referenced by nothing (README, src, docs). Nothing. [/auth-gate-mobile.png]
- `shrink:` `onlyBuiltDependencies` declared twice (package.json pnpm block and pnpm-workspace.yaml, disagreeing on esbuild). Keep the pnpm-workspace.yaml one (v10 canonical home). [-6 lines] [researchquest/package.json, researchquest/pnpm-workspace.yaml]

Kept deliberately: demoSupabase/demoData (documented README feature), the agent-API gateway + agentExplore (documented agent-facing surface with Deno tests in CI), strict-gate canaries (23 lines proving the strict allowlist gate bites), cmdk/radix/virtual/dompurify/fonts (all imported), collections.ts getTopN (now plain filter/sort/slice — pass-3 fix held).

net: -1200 lines, -7 deps possible.
